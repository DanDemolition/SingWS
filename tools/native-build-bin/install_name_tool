#!/bin/bash
# macOS 27 Command Line Tools ship arm64-only support libraries. PyInstaller
# runs under Rosetta for an Intel bundle, so force install_name_tool native.
exec /usr/bin/arch -arm64 /usr/bin/install_name_tool "$@"
